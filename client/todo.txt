01.06.2025

-> fix in private chats -> loading older messages but when messages weren't initialy loaded it can return (and will return) duplicates
    -> fix by after creating (or receiving) new message set appropriate cursor stuff?

-> app notifications (message notifications)

- -- 

fix ui (http://localhost:3000/friends) for smaller sized screens
