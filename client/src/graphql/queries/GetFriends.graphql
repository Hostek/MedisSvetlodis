query GetFriends($input: PaginationCursorArgs!) {
    getFriends(input: $input) {
        totalCount
        pageInfo {
            endCursor
            hasNextPage
            startCursor
        }
        edges {
            cursor
            node {
                id
                identifier
                username
                avatarBgColor
            }
        }
    }
}
