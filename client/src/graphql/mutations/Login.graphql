mutation Login($email: String!, $oauthProof: String) {
    login(email: $email, oauthProof: $oauthProof) {
        user {
            ...UserFragment
        }
        errors {
            ...ErrorFragment
        }
    }
}
