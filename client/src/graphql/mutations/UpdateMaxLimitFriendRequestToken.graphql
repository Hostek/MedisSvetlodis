mutation UpdateMaxLimitFriendRequestToken($tokenId: Int!, $newMaxLimit: Int) {
    updateMaxLimitFriendRequestToken(
        tokenId: $tokenId
        new_max_limit: $newMaxLimit
    ) {
        ...ErrorFragment
    }
}
